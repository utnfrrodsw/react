# Zustand + React

Task manager construido con Zustand para demostrar gestión de estado global en React.

## Conceptos cubiertos

- `create()` — creación de store
- Estado y acciones
- Selectores (suscripción parcial)
- `persist` middleware (localStorage)
- Acciones asincrónicas
- Sin Provider — el hook se consume directo en componentes
